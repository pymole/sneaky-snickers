refresh_rate: 30 seconds

appenders:
  stdout:
    kind: console
    encoder:
      pattern: "{d(%Y-%m-%d %H:%M:%S)} {h({l})} {M} - {m}{n}"

  # stdout:
  #   kind: file
  #   path: "log.log"
  #   encoder:
  #     pattern: "{d(%Y-%m-%d %H:%M:%S)} {h({l})} {M} - {m}{n}"


root:
  level: info
  appenders:
    - stdout
